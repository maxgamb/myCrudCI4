# 2.9.1-dev24-fix11-fix50

Fixes the N:N Related Create offcanvas width variable scope. The N:N template generator now resolves `relationOffcanvasWidth` locally before interpolating the generated markup.
