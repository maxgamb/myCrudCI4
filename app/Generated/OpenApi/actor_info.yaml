openapi: 3.0.3
info:
  title: 'actor_info API'
  version: 1.0.0
  description: 'Generated by myCrudCI4 from the current database schema and persisted CRUD configuration.'
tags:
  - name: 'ActorInfo'
    description: 'Operations for actor_info'
paths:
  /api/v1/actor_info:
    get:
      tags: ['ActorInfo']
      operationId: listActorInfo
      summary: 'List actor_info'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: sort
          in: query
          schema: { type: string, enum: ['actor_id'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc], default: asc }
      responses:
        '200':
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActorInfoListResponse'
        '500':
          $ref: '#/components/responses/InternalError'
components:
  schemas:
    ActorInfoRead:
      type: object
      properties:
        'actor_id':
          type: integer
          format: int32
        'first_name':
          type: string
          maxLength: 45
        'last_name':
          type: string
          maxLength: 45
        'film_info':
          type: string
          nullable: true
          maxLength: 65535
    ActorInfoResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/ActorInfoRead'
        meta: { type: object }
        links: { type: object }
    ActorInfoListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ActorInfoRead'
        meta:
          type: object
          properties:
            page: { type: integer }
            perPage: { type: integer }
            total: { type: integer }
            pageCount: { type: integer }
        links:
          type: object
          properties:
            self: { type: string, nullable: true }
            next: { type: string, nullable: true }
            prev: { type: string, nullable: true }
    ActorInfoCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'actor_id':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
    ActorInfoErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            fields:
              type: object
              additionalProperties:
                type: string
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ActorInfoErrorResponse'
    NotFound:
      description: Record not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ActorInfoErrorResponse'
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ActorInfoErrorResponse'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ActorInfoErrorResponse'
