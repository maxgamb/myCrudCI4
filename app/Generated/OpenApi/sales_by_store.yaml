openapi: 3.0.3
info:
  title: 'sales_by_store API'
  version: 1.0.0
  description: 'Generated by myCrudCI4 from the current database schema and persisted CRUD configuration.'
tags:
  - name: 'SalesByStore'
    description: 'Operations for sales_by_store'
paths:
  /api/v1/sales_by_store:
    get:
      tags: ['SalesByStore']
      operationId: listSalesByStore
      summary: 'List sales_by_store'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: sort
          in: query
          schema: { type: string, enum: ['store'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc], default: asc }
      responses:
        '200':
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesByStoreListResponse'
        '500':
          $ref: '#/components/responses/InternalError'
components:
  schemas:
    SalesByStoreRead:
      type: object
      properties:
        'store':
          type: string
          nullable: true
          maxLength: 101
        'manager':
          type: string
          nullable: true
          maxLength: 91
        'total_sales':
          type: number
          format: double
          nullable: true
    SalesByStoreResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/SalesByStoreRead'
        meta: { type: object }
        links: { type: object }
    SalesByStoreListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/SalesByStoreRead'
        meta:
          type: object
          properties:
            page: { type: integer }
            perPage: { type: integer }
            total: { type: integer }
            pageCount: { type: integer }
        links:
          type: object
          properties:
            self: { type: string, nullable: true }
            next: { type: string, nullable: true }
            prev: { type: string, nullable: true }
    SalesByStoreCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'store':
              type: string
              nullable: true
              maxLength: 101
        meta: { type: object }
        links: { type: object }
    SalesByStoreErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            fields:
              type: object
              additionalProperties:
                type: string
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByStoreErrorResponse'
    NotFound:
      description: Record not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByStoreErrorResponse'
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByStoreErrorResponse'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByStoreErrorResponse'
