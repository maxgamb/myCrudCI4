openapi: 3.0.3
info:
  title: 'film_actor API'
  version: 1.0.0
  description: 'Generated by myCrudCI4 from the current database schema and persisted CRUD configuration.'
tags:
  - name: 'FilmActor'
    description: 'Operations for film_actor'
paths:
  /api/v1/film_actor:
    get:
      tags: ['FilmActor']
      operationId: listFilmActor
      summary: 'List film_actor'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: 'filter[actor_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[film_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: sort
          in: query
          schema: { type: string, enum: ['actor_id', 'film_id'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc], default: asc }
      responses:
        '200':
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilmActorListResponse'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: ['FilmActor']
      operationId: createFilmActor
      summary: 'Create film_actor record'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FilmActorCreate'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilmActorCreatedResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/ValidationError'
        '500':
          $ref: '#/components/responses/InternalError'
components:
  schemas:
    FilmActorRead:
      type: object
      properties:
        'actor_id':
          type: integer
          format: int32
        'film_id':
          type: integer
          format: int32
        'last_update':
          type: string
          format: date-time
        'actor_id__label':
          type: string
          nullable: true
        'film_id__label':
          type: string
          nullable: true
    FilmActorCreate:
      type: object
      required:
        - 'actor_id'
        - 'film_id'
      properties:
        'actor_id':
          type: integer
          format: int32
        'film_id':
          type: integer
          format: int32
    FilmActorResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/FilmActorRead'
        meta: { type: object }
        links: { type: object }
    FilmActorListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/FilmActorRead'
        meta:
          type: object
          properties:
            page: { type: integer }
            perPage: { type: integer }
            total: { type: integer }
            pageCount: { type: integer }
        links:
          type: object
          properties:
            self: { type: string, nullable: true }
            next: { type: string, nullable: true }
            prev: { type: string, nullable: true }
    FilmActorCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'actor_id':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
    FilmActorErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            fields:
              type: object
              additionalProperties:
                type: string
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmActorErrorResponse'
    NotFound:
      description: Record not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmActorErrorResponse'
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmActorErrorResponse'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmActorErrorResponse'
