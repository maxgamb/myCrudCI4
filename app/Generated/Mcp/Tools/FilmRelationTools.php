<?php

declare(strict_types=1);

namespace App\Mcp\Tools;

use App\Models\FilmModel;
use Mcp\Capability\Attribute\McpTool;

/**
 * Read-only relation tools generated by myCrudCI4 for film.
 */
final class FilmRelationTools
{
    public function __construct(private readonly FilmModel $model = new FilmModel())
    {
    }

    /**
     * Returns the belongsTo language relation associated with film.language_id.
     *
     * Reads the film record, retrieves the foreign-key value, and resolves the label
     * configured through the generated Model.
     *
     * @param string $id Primary key of the film record.
     *
     * @return array{
     *     field: string,
     *     parentTable: string,
     *     value: string|null,
     *     relation: array<string,mixed>|null
     * }
     */
    #[McpTool(
        name: 'get_film_language_id',
        title: 'Get language relation for film.language_id'
    )]
    public function getFilmLanguageId(string $id): array
    {
        $record = $this->model->getDetail($id);
        if (!is_object($record)) {
            throw new \InvalidArgumentException('Record not found.');
        }
        $foreignId = is_array($record)
            ? ($record['language_id'] ?? null)
            : ($record->language_id ?? null);

        if ($foreignId === null || $foreignId === '') {
            return [
                'field' => 'language_id',
                'parentTable' => 'language',
                'value' => null,
                'relation' => null,
            ];
        }

        return [
            'field' => 'language_id',
            'parentTable' => 'language',
            'value' => (string) $foreignId,
            'relation' => $this->model->relationOptionById('language_id', $foreignId),
        ];
    }

    /**
     * Returns the belongsTo language relation associated with film.original_language_id.
     *
     * Reads the film record, retrieves the foreign-key value, and resolves the label
     * configured through the generated Model.
     *
     * @param string $id Primary key of the film record.
     *
     * @return array{
     *     field: string,
     *     parentTable: string,
     *     value: string|null,
     *     relation: array<string,mixed>|null
     * }
     */
    #[McpTool(
        name: 'get_film_original_language_id',
        title: 'Get language relation for film.original_language_id'
    )]
    public function getFilmOriginalLanguageId(string $id): array
    {
        $record = $this->model->getDetail($id);
        if (!is_object($record)) {
            throw new \InvalidArgumentException('Record not found.');
        }
        $foreignId = is_array($record)
            ? ($record['original_language_id'] ?? null)
            : ($record->original_language_id ?? null);

        if ($foreignId === null || $foreignId === '') {
            return [
                'field' => 'original_language_id',
                'parentTable' => 'language',
                'value' => null,
                'relation' => null,
            ];
        }

        return [
            'field' => 'original_language_id',
            'parentTable' => 'language',
            'value' => (string) $foreignId,
            'relation' => $this->model->relationOptionById('original_language_id', $foreignId),
        ];
    }

    /**
     * Returns the film_actor records related to the film record.
     *
     * Uses the hasMany scaffold generated by the Model and preserves
     * the limit, count, and hasMore values defined by the relation configuration.
     *
     * @param string $id Primary key of the film record.
     *
     * @return array{
     *     relation: string,
     *     childTable: string,
     *     foreignKey: string,
     *     rows: array<int,mixed>,
     *     count: int,
     *     hasMore: bool
     * }
     */
    #[McpTool(
        name: 'list_film_film_actor_by_film_id',
        title: 'List film_actor related to film'
    )]
    public function listFilmFilmActorFilmId(string $id): array
    {
        $relations = $this->model->loadHasMany($id);
        $result = (array) ($relations['film_actor__film_id'] ?? []);

        return [
            'relation' => 'film_actor__film_id',
            'childTable' => 'film_actor',
            'foreignKey' => 'film_id',
            'rows' => array_values((array) ($result['rows'] ?? [])),
            'count' => (int) ($result['count'] ?? 0),
            'hasMore' => !empty($result['hasMore']),
        ];
    }

    /**
     * Returns the film_category records related to the film record.
     *
     * Uses the hasMany scaffold generated by the Model and preserves
     * the limit, count, and hasMore values defined by the relation configuration.
     *
     * @param string $id Primary key of the film record.
     *
     * @return array{
     *     relation: string,
     *     childTable: string,
     *     foreignKey: string,
     *     rows: array<int,mixed>,
     *     count: int,
     *     hasMore: bool
     * }
     */
    #[McpTool(
        name: 'list_film_film_category_by_film_id',
        title: 'List film_category related to film'
    )]
    public function listFilmFilmCategoryFilmId(string $id): array
    {
        $relations = $this->model->loadHasMany($id);
        $result = (array) ($relations['film_category__film_id'] ?? []);

        return [
            'relation' => 'film_category__film_id',
            'childTable' => 'film_category',
            'foreignKey' => 'film_id',
            'rows' => array_values((array) ($result['rows'] ?? [])),
            'count' => (int) ($result['count'] ?? 0),
            'hasMore' => !empty($result['hasMore']),
        ];
    }

    /**
     * Returns the inventory records related to the film record.
     *
     * Uses the hasMany scaffold generated by the Model and preserves
     * the limit, count, and hasMore values defined by the relation configuration.
     *
     * @param string $id Primary key of the film record.
     *
     * @return array{
     *     relation: string,
     *     childTable: string,
     *     foreignKey: string,
     *     rows: array<int,mixed>,
     *     count: int,
     *     hasMore: bool
     * }
     */
    #[McpTool(
        name: 'list_film_inventory_by_film_id',
        title: 'List inventory related to film'
    )]
    public function listFilmInventoryFilmId(string $id): array
    {
        $relations = $this->model->loadHasMany($id);
        $result = (array) ($relations['inventory__film_id'] ?? []);

        return [
            'relation' => 'inventory__film_id',
            'childTable' => 'inventory',
            'foreignKey' => 'film_id',
            'rows' => array_values((array) ($result['rows'] ?? [])),
            'count' => (int) ($result['count'] ?? 0),
            'hasMore' => !empty($result['hasMore']),
        ];
    }
}
