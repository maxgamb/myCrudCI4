<?php

declare(strict_types=1);

namespace App\Mcp\Tools;

use App\Models\CustomerModel;
use Mcp\Capability\Attribute\McpTool;

/**
 * Read-only relation tools generated by myCrudCI4 for customer.
 */
final class CustomerRelationTools
{
    public function __construct(private readonly CustomerModel $model = new CustomerModel())
    {
    }

    /**
     * Returns the belongsTo address relation associated with customer.address_id.
     *
     * Reads the customer record, retrieves the foreign-key value, and resolves the label
     * configured through the generated Model.
     *
     * @param string $id Primary key of the customer record.
     *
     * @return array{
     *     field: string,
     *     parentTable: string,
     *     value: string|null,
     *     relation: array<string,mixed>|null
     * }
     */
    #[McpTool(
        name: 'get_customer_address_id',
        title: 'Get address relation for customer.address_id'
    )]
    public function getCustomerAddressId(string $id): array
    {
        $record = $this->model->getDetail($id);
        if (!is_object($record)) {
            throw new \InvalidArgumentException('Record not found.');
        }
        $foreignId = is_array($record)
            ? ($record['address_id'] ?? null)
            : ($record->address_id ?? null);

        if ($foreignId === null || $foreignId === '') {
            return [
                'field' => 'address_id',
                'parentTable' => 'address',
                'value' => null,
                'relation' => null,
            ];
        }

        return [
            'field' => 'address_id',
            'parentTable' => 'address',
            'value' => (string) $foreignId,
            'relation' => $this->model->relationOptionById('address_id', $foreignId),
        ];
    }

    /**
     * Returns the belongsTo store relation associated with customer.store_id.
     *
     * Reads the customer record, retrieves the foreign-key value, and resolves the label
     * configured through the generated Model.
     *
     * @param string $id Primary key of the customer record.
     *
     * @return array{
     *     field: string,
     *     parentTable: string,
     *     value: string|null,
     *     relation: array<string,mixed>|null
     * }
     */
    #[McpTool(
        name: 'get_customer_store_id',
        title: 'Get store relation for customer.store_id'
    )]
    public function getCustomerStoreId(string $id): array
    {
        $record = $this->model->getDetail($id);
        if (!is_object($record)) {
            throw new \InvalidArgumentException('Record not found.');
        }
        $foreignId = is_array($record)
            ? ($record['store_id'] ?? null)
            : ($record->store_id ?? null);

        if ($foreignId === null || $foreignId === '') {
            return [
                'field' => 'store_id',
                'parentTable' => 'store',
                'value' => null,
                'relation' => null,
            ];
        }

        return [
            'field' => 'store_id',
            'parentTable' => 'store',
            'value' => (string) $foreignId,
            'relation' => $this->model->relationOptionById('store_id', $foreignId),
        ];
    }

    /**
     * Returns the payment records related to the customer record.
     *
     * Uses the hasMany scaffold generated by the Model and preserves
     * the limit, count, and hasMore values defined by the relation configuration.
     *
     * @param string $id Primary key of the customer record.
     *
     * @return array{
     *     relation: string,
     *     childTable: string,
     *     foreignKey: string,
     *     rows: array<int,mixed>,
     *     count: int,
     *     hasMore: bool
     * }
     */
    #[McpTool(
        name: 'list_customer_payment_by_customer_id',
        title: 'List payment related to customer'
    )]
    public function listCustomerPaymentCustomerId(string $id): array
    {
        $relations = $this->model->loadHasMany($id);
        $result = (array) ($relations['payment__customer_id'] ?? []);

        return [
            'relation' => 'payment__customer_id',
            'childTable' => 'payment',
            'foreignKey' => 'customer_id',
            'rows' => array_values((array) ($result['rows'] ?? [])),
            'count' => (int) ($result['count'] ?? 0),
            'hasMore' => !empty($result['hasMore']),
        ];
    }

    /**
     * Returns the rental records related to the customer record.
     *
     * Uses the hasMany scaffold generated by the Model and preserves
     * the limit, count, and hasMore values defined by the relation configuration.
     *
     * @param string $id Primary key of the customer record.
     *
     * @return array{
     *     relation: string,
     *     childTable: string,
     *     foreignKey: string,
     *     rows: array<int,mixed>,
     *     count: int,
     *     hasMore: bool
     * }
     */
    #[McpTool(
        name: 'list_customer_rental_by_customer_id',
        title: 'List rental related to customer'
    )]
    public function listCustomerRentalCustomerId(string $id): array
    {
        $relations = $this->model->loadHasMany($id);
        $result = (array) ($relations['rental__customer_id'] ?? []);

        return [
            'relation' => 'rental__customer_id',
            'childTable' => 'rental',
            'foreignKey' => 'customer_id',
            'rows' => array_values((array) ($result['rows'] ?? [])),
            'count' => (int) ($result['count'] ?? 0),
            'hasMore' => !empty($result['hasMore']),
        ];
    }
}
