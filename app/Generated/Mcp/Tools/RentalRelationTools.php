<?php

declare(strict_types=1);

namespace App\Mcp\Tools;

use App\Models\RentalModel;
use Mcp\Capability\Attribute\McpTool;

/**
 * Read-only relation tools generated by myCrudCI4 for rental.
 */
final class RentalRelationTools
{
    public function __construct(private readonly RentalModel $model = new RentalModel())
    {
    }

    /**
     * Returns the belongsTo customer relation associated with rental.customer_id.
     *
     * Reads the rental record, retrieves the foreign-key value, and resolves the label
     * configured through the generated Model.
     *
     * @param string $id Primary key of the rental record.
     *
     * @return array{
     *     field: string,
     *     parentTable: string,
     *     value: string|null,
     *     relation: array<string,mixed>|null
     * }
     */
    #[McpTool(
        name: 'get_rental_customer_id',
        title: 'Get customer relation for rental.customer_id'
    )]
    public function getRentalCustomerId(string $id): array
    {
        $record = $this->model->getDetail($id);
        if (!is_object($record)) {
            throw new \InvalidArgumentException('Record not found.');
        }
        $foreignId = is_array($record)
            ? ($record['customer_id'] ?? null)
            : ($record->customer_id ?? null);

        if ($foreignId === null || $foreignId === '') {
            return [
                'field' => 'customer_id',
                'parentTable' => 'customer',
                'value' => null,
                'relation' => null,
            ];
        }

        return [
            'field' => 'customer_id',
            'parentTable' => 'customer',
            'value' => (string) $foreignId,
            'relation' => $this->model->relationOptionById('customer_id', $foreignId),
        ];
    }

    /**
     * Returns the belongsTo inventory relation associated with rental.inventory_id.
     *
     * Reads the rental record, retrieves the foreign-key value, and resolves the label
     * configured through the generated Model.
     *
     * @param string $id Primary key of the rental record.
     *
     * @return array{
     *     field: string,
     *     parentTable: string,
     *     value: string|null,
     *     relation: array<string,mixed>|null
     * }
     */
    #[McpTool(
        name: 'get_rental_inventory_id',
        title: 'Get inventory relation for rental.inventory_id'
    )]
    public function getRentalInventoryId(string $id): array
    {
        $record = $this->model->getDetail($id);
        if (!is_object($record)) {
            throw new \InvalidArgumentException('Record not found.');
        }
        $foreignId = is_array($record)
            ? ($record['inventory_id'] ?? null)
            : ($record->inventory_id ?? null);

        if ($foreignId === null || $foreignId === '') {
            return [
                'field' => 'inventory_id',
                'parentTable' => 'inventory',
                'value' => null,
                'relation' => null,
            ];
        }

        return [
            'field' => 'inventory_id',
            'parentTable' => 'inventory',
            'value' => (string) $foreignId,
            'relation' => $this->model->relationOptionById('inventory_id', $foreignId),
        ];
    }

    /**
     * Returns the belongsTo staff relation associated with rental.staff_id.
     *
     * Reads the rental record, retrieves the foreign-key value, and resolves the label
     * configured through the generated Model.
     *
     * @param string $id Primary key of the rental record.
     *
     * @return array{
     *     field: string,
     *     parentTable: string,
     *     value: string|null,
     *     relation: array<string,mixed>|null
     * }
     */
    #[McpTool(
        name: 'get_rental_staff_id',
        title: 'Get staff relation for rental.staff_id'
    )]
    public function getRentalStaffId(string $id): array
    {
        $record = $this->model->getDetail($id);
        if (!is_object($record)) {
            throw new \InvalidArgumentException('Record not found.');
        }
        $foreignId = is_array($record)
            ? ($record['staff_id'] ?? null)
            : ($record->staff_id ?? null);

        if ($foreignId === null || $foreignId === '') {
            return [
                'field' => 'staff_id',
                'parentTable' => 'staff',
                'value' => null,
                'relation' => null,
            ];
        }

        return [
            'field' => 'staff_id',
            'parentTable' => 'staff',
            'value' => (string) $foreignId,
            'relation' => $this->model->relationOptionById('staff_id', $foreignId),
        ];
    }

    /**
     * Returns the payment records related to the rental record.
     *
     * Uses the hasMany scaffold generated by the Model and preserves
     * the limit, count, and hasMore values defined by the relation configuration.
     *
     * @param string $id Primary key of the rental record.
     *
     * @return array{
     *     relation: string,
     *     childTable: string,
     *     foreignKey: string,
     *     rows: array<int,mixed>,
     *     count: int,
     *     hasMore: bool
     * }
     */
    #[McpTool(
        name: 'list_rental_payment_by_rental_id',
        title: 'List payment related to rental'
    )]
    public function listRentalPaymentRentalId(string $id): array
    {
        $relations = $this->model->loadHasMany($id);
        $result = (array) ($relations['payment__rental_id'] ?? []);

        return [
            'relation' => 'payment__rental_id',
            'childTable' => 'payment',
            'foreignKey' => 'rental_id',
            'rows' => array_values((array) ($result['rows'] ?? [])),
            'count' => (int) ($result['count'] ?? 0),
            'hasMore' => !empty($result['hasMore']),
        ];
    }
}
