openapi: 3.0.3
info:
  title: 'sales_by_film_category API'
  version: 1.0.0
  description: 'Generated by myCrudCI4 from the current database schema and persisted CRUD configuration.'
tags:
  - name: 'SalesByFilmCategory'
    description: 'Operations for sales_by_film_category'
paths:
  /api/v1/sales_by_film_category:
    get:
      tags: ['SalesByFilmCategory']
      operationId: listSalesByFilmCategory
      summary: 'List sales_by_film_category'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: sort
          in: query
          schema: { type: string, enum: ['category'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc], default: asc }
      responses:
        '200':
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesByFilmCategoryListResponse'
        '500':
          $ref: '#/components/responses/InternalError'
components:
  schemas:
    SalesByFilmCategoryRead:
      type: object
      properties:
        'category':
          type: string
          maxLength: 25
        'total_sales':
          type: number
          format: double
          nullable: true
    SalesByFilmCategoryResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/SalesByFilmCategoryRead'
        meta: { type: object }
        links: { type: object }
    SalesByFilmCategoryListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/SalesByFilmCategoryRead'
        meta:
          type: object
          properties:
            page: { type: integer }
            perPage: { type: integer }
            total: { type: integer }
            pageCount: { type: integer }
        links:
          type: object
          properties:
            self: { type: string, nullable: true }
            next: { type: string, nullable: true }
            prev: { type: string, nullable: true }
    SalesByFilmCategoryCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'category':
              type: string
              maxLength: 25
        meta: { type: object }
        links: { type: object }
    SalesByFilmCategoryErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            fields:
              type: object
              additionalProperties:
                type: string
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByFilmCategoryErrorResponse'
    NotFound:
      description: Record not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByFilmCategoryErrorResponse'
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByFilmCategoryErrorResponse'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SalesByFilmCategoryErrorResponse'
