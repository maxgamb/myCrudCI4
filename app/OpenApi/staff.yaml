openapi: 3.0.3
info:
  title: 'staff API'
  version: 1.0.0
  description: 'Generated by myCrudCI4 from the current database schema and persisted CRUD configuration.'
tags:
  - name: 'Staff'
    description: 'Operations for staff'
paths:
  /api/v1/staff:
    get:
      tags: ['Staff']
      operationId: listStaff
      summary: 'List staff'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: 'filter[staff_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[address_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[store_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: sort
          in: query
          schema: { type: string, enum: ['staff_id', 'address_id', 'store_id'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc], default: asc }
      responses:
        '200':
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffListResponse'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: ['Staff']
      operationId: createStaff
      summary: 'Create staff record'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StaffCreate'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffCreatedResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/ValidationError'
        '500':
          $ref: '#/components/responses/InternalError'
  /api/v1/staff/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int32
    get:
      tags: ['Staff']
      operationId: getStaff
      summary: 'Get staff record'
      responses:
        '200':
          description: Record found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    put:
      tags: ['Staff']
      operationId: updateStaff
      summary: 'Replace/update staff record'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StaffUpdate'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      tags: ['Staff']
      operationId: patchStaff
      summary: 'Partially update staff record'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StaffPatch'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StaffResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      tags: ['Staff']
      operationId: deleteStaff
      summary: 'Delete staff record'
      responses:
        '204': { description: Deleted }
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
components:
  schemas:
    StaffRead:
      type: object
      properties:
        'staff_id':
          type: integer
          format: int32
        'first_name':
          type: string
          maxLength: 45
        'last_name':
          type: string
          maxLength: 45
        'address_id':
          type: integer
          format: int32
        'email':
          type: string
          nullable: true
          maxLength: 50
        'store_id':
          type: integer
          format: int32
        'active':
          type: boolean
        'username':
          type: string
          maxLength: 16
        'password':
          type: string
          nullable: true
          maxLength: 40
        'last_update':
          type: string
          format: date-time
        'address_id__label':
          type: string
          nullable: true
        'store_id__label':
          type: string
          nullable: true
    StaffCreate:
      type: object
      required:
        - 'first_name'
        - 'last_name'
        - 'address_id'
        - 'store_id'
        - 'username'
      properties:
        'first_name':
          type: string
          maxLength: 45
        'last_name':
          type: string
          maxLength: 45
        'address_id':
          type: integer
          format: int32
        'picture':
          type: string
          format: byte
          nullable: true
          maxLength: 65535
        'email':
          type: string
          nullable: true
          maxLength: 50
        'store_id':
          type: integer
          format: int32
        'active':
          type: boolean
        'username':
          type: string
          maxLength: 16
        'password':
          type: string
          nullable: true
          maxLength: 40
    StaffUpdate:
      type: object
      required:
        - 'first_name'
        - 'last_name'
        - 'address_id'
        - 'store_id'
        - 'username'
      properties:
        'first_name':
          type: string
          maxLength: 45
        'last_name':
          type: string
          maxLength: 45
        'address_id':
          type: integer
          format: int32
        'picture':
          type: string
          format: byte
          nullable: true
          maxLength: 65535
        'email':
          type: string
          nullable: true
          maxLength: 50
        'store_id':
          type: integer
          format: int32
        'active':
          type: boolean
        'username':
          type: string
          maxLength: 16
        'password':
          type: string
          nullable: true
          maxLength: 40
    StaffPatch:
      type: object
      properties:
        'first_name':
          type: string
          maxLength: 45
        'last_name':
          type: string
          maxLength: 45
        'address_id':
          type: integer
          format: int32
        'picture':
          type: string
          format: byte
          nullable: true
          maxLength: 65535
        'email':
          type: string
          nullable: true
          maxLength: 50
        'store_id':
          type: integer
          format: int32
        'active':
          type: boolean
        'username':
          type: string
          maxLength: 16
        'password':
          type: string
          nullable: true
          maxLength: 40
    StaffResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/StaffRead'
        meta: { type: object }
        links: { type: object }
    StaffListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/StaffRead'
        meta:
          type: object
          properties:
            page: { type: integer }
            perPage: { type: integer }
            total: { type: integer }
            pageCount: { type: integer }
        links:
          type: object
          properties:
            self: { type: string, nullable: true }
            next: { type: string, nullable: true }
            prev: { type: string, nullable: true }
    StaffCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'staff_id':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
    StaffErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            fields:
              type: object
              additionalProperties:
                type: string
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StaffErrorResponse'
    NotFound:
      description: Record not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StaffErrorResponse'
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StaffErrorResponse'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StaffErrorResponse'
