openapi: 3.0.3
info:
  title: 'film_category API'
  version: 1.0.0
  description: 'Generated by myCrudCI4 from the current database schema and persisted CRUD configuration.'
tags:
  - name: 'FilmCategory'
    description: 'Operations for film_category'
paths:
  /api/v1/film_category:
    get:
      tags: ['FilmCategory']
      operationId: listFilmCategory
      summary: 'List film_category'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: 'filter[film_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[category_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: sort
          in: query
          schema: { type: string, enum: ['film_id', 'category_id'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc], default: asc }
      responses:
        '200':
          description: Paginated list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilmCategoryListResponse'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: ['FilmCategory']
      operationId: createFilmCategory
      summary: 'Create film_category record'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FilmCategoryCreate'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilmCategoryCreatedResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/ValidationError'
        '500':
          $ref: '#/components/responses/InternalError'
components:
  schemas:
    FilmCategoryRead:
      type: object
      properties:
        'film_id':
          type: integer
          format: int32
        'category_id':
          type: integer
          format: int32
        'last_update':
          type: string
          format: date-time
        'category_id__label':
          type: string
          nullable: true
        'film_id__label':
          type: string
          nullable: true
    FilmCategoryCreate:
      type: object
      required:
        - 'film_id'
        - 'category_id'
      properties:
        'film_id':
          type: integer
          format: int32
        'category_id':
          type: integer
          format: int32
    FilmCategoryResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/FilmCategoryRead'
        meta: { type: object }
        links: { type: object }
    FilmCategoryListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/FilmCategoryRead'
        meta:
          type: object
          properties:
            page: { type: integer }
            perPage: { type: integer }
            total: { type: integer }
            pageCount: { type: integer }
        links:
          type: object
          properties:
            self: { type: string, nullable: true }
            next: { type: string, nullable: true }
            prev: { type: string, nullable: true }
    FilmCategoryCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'film_id':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
    FilmCategoryErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            fields:
              type: object
              additionalProperties:
                type: string
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmCategoryErrorResponse'
    NotFound:
      description: Record not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmCategoryErrorResponse'
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmCategoryErrorResponse'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FilmCategoryErrorResponse'
