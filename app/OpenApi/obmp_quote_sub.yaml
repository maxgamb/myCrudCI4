openapi: 3.0.3
info:
  title: 'obmp_quote_sub API'
  version: 1.0.0
paths:
  /api/v1/obmp_quote_sub:
    get:
      summary: 'Elenco obmp_quote_sub'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: 'filter[obmp_quote_sub_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[obmp_quote_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: sort
          in: query
          schema: { type: string, enum: ['obmp_quote_sub_id', 'obmp_quote_id'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc] }
      responses:
        '200':
          description: Elenco paginato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObmpQuoteSubListResponse'
    post:
      summary: Crea record
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ObmpQuoteSubCreate'
      responses:
        '201':
          description: Record creato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ObmpQuoteSubCreatedResponse'
        '422': { description: Errore di validazione }
  /api/v1/obmp_quote_sub/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      summary: Dettaglio record
      responses:
        '200':
          description: Record trovato
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ObmpQuoteSubRead'
        '404': { description: Record non trovato }
    put:
      summary: Aggiornamento completo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ObmpQuoteSubUpdate'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    patch:
      summary: Aggiornamento parziale
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ObmpQuoteSubPatch'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    delete:
      summary: Elimina record
      responses:
        '204': { description: Record eliminato }
components:
  schemas:
    ObmpQuoteSubRead:
      type: object
      properties:
        'obmp_quote_sub_id':
          type: integer
          format: int32
        'obmp_quote_id':
          type: integer
          format: int32
        'hotel_id':
          type: integer
          format: int32
        'quote_sub_jeson':
          type: string
          maxLength: 255
        'quote_sub_data':
          type: string
          format: date
        'randomd_string':
          type: string
          maxLength: 100
        'obmp_quote__obmp_quote_id__label':
          type: string
          nullable: true
    ObmpQuoteSubCreate:
      type: object
      required:
        - 'obmp_quote_id'
        - 'hotel_id'
        - 'quote_sub_jeson'
        - 'quote_sub_data'
        - 'randomd_string'
      properties:
        'obmp_quote_id':
          type: integer
          format: int32
        'hotel_id':
          type: integer
          format: int32
        'quote_sub_jeson':
          type: string
          maxLength: 255
        'quote_sub_data':
          type: string
          format: date
        'randomd_string':
          type: string
          maxLength: 100
    ObmpQuoteSubUpdate:
      type: object
      required:
        - 'obmp_quote_id'
        - 'hotel_id'
        - 'quote_sub_jeson'
        - 'quote_sub_data'
        - 'randomd_string'
      properties:
        'obmp_quote_id':
          type: integer
          format: int32
        'hotel_id':
          type: integer
          format: int32
        'quote_sub_jeson':
          type: string
          maxLength: 255
        'quote_sub_data':
          type: string
          format: date
        'randomd_string':
          type: string
          maxLength: 100
    ObmpQuoteSubPatch:
      type: object
      properties:
        'obmp_quote_id':
          type: integer
          format: int32
        'hotel_id':
          type: integer
          format: int32
        'quote_sub_jeson':
          type: string
          maxLength: 255
        'quote_sub_data':
          type: string
          format: date
        'randomd_string':
          type: string
          maxLength: 100
    ObmpQuoteSubListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ObmpQuoteSubRead'
        meta: { type: object }
        links: { type: object }
    ObmpQuoteSubCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'obmp_quote_sub_id':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
