openapi: 3.0.3
info:
  title: 'ref_agenda_clienti API'
  version: 1.0.0
paths:
  /api/v1/ref_agenda_clienti:
    get:
      summary: 'Elenco ref_agenda_clienti'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: 'filter[ref_agenda_cliente]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[preno_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[clienti_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: sort
          in: query
          schema: { type: string, enum: ['ref_agenda_cliente', 'preno_id', 'clienti_id'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc] }
      responses:
        '200':
          description: Elenco paginato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefAgendaClientiListResponse'
    post:
      summary: Crea record
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefAgendaClientiCreate'
      responses:
        '201':
          description: Record creato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefAgendaClientiCreatedResponse'
        '422': { description: Errore di validazione }
  /api/v1/ref_agenda_clienti/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      summary: Dettaglio record
      responses:
        '200':
          description: Record trovato
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RefAgendaClientiRead'
        '404': { description: Record non trovato }
    put:
      summary: Aggiornamento completo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefAgendaClientiUpdate'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    patch:
      summary: Aggiornamento parziale
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefAgendaClientiPatch'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    delete:
      summary: Elimina record
      responses:
        '204': { description: Record eliminato }
components:
  schemas:
    RefAgendaClientiRead:
      type: object
      properties:
        'ref_agenda_cliente':
          type: integer
          format: int32
        'preno_id':
          type: integer
          format: int32
        'clienti_id':
          type: integer
          format: int32
        'tipologia_id':
          type: integer
          format: int32
        'ref_a_c_datarecord':
          type: string
          format: date-time
        'agenda__preno_id__label':
          type: string
          nullable: true
    RefAgendaClientiCreate:
      type: object
      required:
        - 'preno_id'
        - 'clienti_id'
        - 'tipologia_id'
      properties:
        'preno_id':
          type: integer
          format: int32
        'clienti_id':
          type: integer
          format: int32
        'tipologia_id':
          type: integer
          format: int32
        'ref_a_c_datarecord':
          type: string
          format: date-time
    RefAgendaClientiUpdate:
      type: object
      required:
        - 'preno_id'
        - 'clienti_id'
        - 'tipologia_id'
      properties:
        'preno_id':
          type: integer
          format: int32
        'clienti_id':
          type: integer
          format: int32
        'tipologia_id':
          type: integer
          format: int32
        'ref_a_c_datarecord':
          type: string
          format: date-time
    RefAgendaClientiPatch:
      type: object
      properties:
        'preno_id':
          type: integer
          format: int32
        'clienti_id':
          type: integer
          format: int32
        'tipologia_id':
          type: integer
          format: int32
        'ref_a_c_datarecord':
          type: string
          format: date-time
    RefAgendaClientiListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/RefAgendaClientiRead'
        meta: { type: object }
        links: { type: object }
    RefAgendaClientiCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'ref_agenda_cliente':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
