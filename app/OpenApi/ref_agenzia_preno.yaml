openapi: 3.0.3
info:
  title: 'ref_agenzia_preno API'
  version: 1.0.0
paths:
  /api/v1/ref_agenzia_preno:
    get:
      summary: 'Elenco ref_agenzia_preno'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: 'filter[ref_agenzia_preno]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[agenzia_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[preno_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: sort
          in: query
          schema: { type: string, enum: ['ref_agenzia_preno', 'agenzia_id', 'preno_id'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc] }
      responses:
        '200':
          description: Elenco paginato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefAgenziaPrenoListResponse'
    post:
      summary: Crea record
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefAgenziaPrenoCreate'
      responses:
        '201':
          description: Record creato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefAgenziaPrenoCreatedResponse'
        '422': { description: Errore di validazione }
  /api/v1/ref_agenzia_preno/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      summary: Dettaglio record
      responses:
        '200':
          description: Record trovato
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RefAgenziaPrenoRead'
        '404': { description: Record non trovato }
    put:
      summary: Aggiornamento completo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefAgenziaPrenoUpdate'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    patch:
      summary: Aggiornamento parziale
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefAgenziaPrenoPatch'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    delete:
      summary: Elimina record
      responses:
        '204': { description: Record eliminato }
components:
  schemas:
    RefAgenziaPrenoRead:
      type: object
      properties:
        'ref_agenzia_preno':
          type: integer
          format: int32
        'agenzia_id':
          type: integer
          format: int32
        'preno_id':
          type: integer
          format: int32
        'ref_a_p_datarecord':
          type: string
          format: date-time
        'agenda__preno_id__label':
          type: string
          nullable: true
        'agenzie__agenzia_id__label':
          type: string
          nullable: true
    RefAgenziaPrenoCreate:
      type: object
      required:
        - 'agenzia_id'
        - 'preno_id'
      properties:
        'agenzia_id':
          type: integer
          format: int32
        'preno_id':
          type: integer
          format: int32
        'ref_a_p_datarecord':
          type: string
          format: date-time
    RefAgenziaPrenoUpdate:
      type: object
      required:
        - 'agenzia_id'
        - 'preno_id'
      properties:
        'agenzia_id':
          type: integer
          format: int32
        'preno_id':
          type: integer
          format: int32
        'ref_a_p_datarecord':
          type: string
          format: date-time
    RefAgenziaPrenoPatch:
      type: object
      properties:
        'agenzia_id':
          type: integer
          format: int32
        'preno_id':
          type: integer
          format: int32
        'ref_a_p_datarecord':
          type: string
          format: date-time
    RefAgenziaPrenoListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/RefAgenziaPrenoRead'
        meta: { type: object }
        links: { type: object }
    RefAgenziaPrenoCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'ref_agenzia_preno':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
