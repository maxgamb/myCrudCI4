openapi: 3.0.3
info:
  title: 'listino_periodi_obmp API'
  version: 1.0.0
paths:
  /api/v1/listino_periodi_obmp:
    get:
      summary: 'Elenco listino_periodi_obmp'
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: 'filter[listino_periodi_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[listino_nome_id]'
          in: query
          schema:
            type: integer
            format: int32
        - name: 'filter[listino_dal]'
          in: query
          schema:
            type: string
            format: date
        - name: sort
          in: query
          schema: { type: string, enum: ['listino_periodi_id', 'listino_nome_id', 'listino_dal'] }
        - name: direction
          in: query
          schema: { type: string, enum: [asc, desc] }
      responses:
        '200':
          description: Elenco paginato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListinoPeriodiObmpListResponse'
    post:
      summary: Crea record
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListinoPeriodiObmpCreate'
      responses:
        '201':
          description: Record creato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListinoPeriodiObmpCreatedResponse'
        '422': { description: Errore di validazione }
  /api/v1/listino_periodi_obmp/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      summary: Dettaglio record
      responses:
        '200':
          description: Record trovato
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ListinoPeriodiObmpRead'
        '404': { description: Record non trovato }
    put:
      summary: Aggiornamento completo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListinoPeriodiObmpUpdate'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    patch:
      summary: Aggiornamento parziale
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListinoPeriodiObmpPatch'
      responses:
        '200': { description: Record aggiornato }
        '422': { description: Errore di validazione }
    delete:
      summary: Elimina record
      responses:
        '204': { description: Record eliminato }
components:
  schemas:
    ListinoPeriodiObmpRead:
      type: object
      properties:
        'listino_periodi_id':
          type: integer
          format: int32
        'listino_nome_id':
          type: integer
          format: int32
        'listino_periodi_flex':
          type: integer
          format: int32
          nullable: true
        'listino_dal':
          type: string
          format: date
        'listino_al':
          type: string
          format: date
        'hotel_id':
          type: integer
          format: int32
        'listino_periodi':
          type: string
          format: date-time
        'listino_nome_obmp__listino_nome_id__label':
          type: string
          nullable: true
    ListinoPeriodiObmpCreate:
      type: object
      required:
        - 'listino_dal'
        - 'listino_al'
      properties:
        'listino_nome_id':
          type: integer
          format: int32
        'listino_periodi_flex':
          type: integer
          format: int32
          nullable: true
        'listino_dal':
          type: string
          format: date
        'listino_al':
          type: string
          format: date
        'hotel_id':
          type: integer
          format: int32
        'listino_periodi':
          type: string
          format: date-time
    ListinoPeriodiObmpUpdate:
      type: object
      required:
        - 'listino_dal'
        - 'listino_al'
      properties:
        'listino_nome_id':
          type: integer
          format: int32
        'listino_periodi_flex':
          type: integer
          format: int32
          nullable: true
        'listino_dal':
          type: string
          format: date
        'listino_al':
          type: string
          format: date
        'hotel_id':
          type: integer
          format: int32
        'listino_periodi':
          type: string
          format: date-time
    ListinoPeriodiObmpPatch:
      type: object
      properties:
        'listino_nome_id':
          type: integer
          format: int32
        'listino_periodi_flex':
          type: integer
          format: int32
          nullable: true
        'listino_dal':
          type: string
          format: date
        'listino_al':
          type: string
          format: date
        'hotel_id':
          type: integer
          format: int32
        'listino_periodi':
          type: string
          format: date-time
    ListinoPeriodiObmpListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ListinoPeriodiObmpRead'
        meta: { type: object }
        links: { type: object }
    ListinoPeriodiObmpCreatedResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            'listino_periodi_id':
              type: integer
              format: int32
        meta: { type: object }
        links: { type: object }
