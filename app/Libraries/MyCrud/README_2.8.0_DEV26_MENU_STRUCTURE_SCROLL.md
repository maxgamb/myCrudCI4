# myCrudGpt 2.8.0-dev26 — Menu Structure scroll

Bugfix UI minimale del Menu Builder.

- Il pannello **Struttura menu** usa ora la stessa area verticale scorrevole di **Voci non assegnate**.
- Altezza desktop: `calc(100vh - 330px)`.
- Altezza massima sotto 1200px: `520px`.
- Nessuna modifica a drag&drop, salvataggio, generazione menu o configurazioni persistenti.
